* Fix for Plex not using latest Radeon drivers *

If you are using a non-default path for the Plex Application Support directory, define it in a file ".env" as "PLEX_APPLICATION_SUPPORT_DIR=<app_support_path>".  Then run "run".
